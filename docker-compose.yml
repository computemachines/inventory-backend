version: '3'
services:
  # nginx servers static files and is a proxy for api-endpoint
  nginx:
    build: nginx
    ports:
      - '8080:80'
  api-endpoint:
    build: uwsgi-api-server
    # ports:
    #   - '80:5000'
  mongo:
    image: mongo
